# DeepX-OS

A modern operating system architected for safety and flexibility, leveraging the performance of Rust and the extensibility of Lua, powered by the Limine bootloader.

Build and run with: ./run.sh (works only linux)
